# ── Generated entity files (runtime output — not source) ─────────────────────
data/**/*.md



# ── Electron / Node build artefacts ──────────────────────────────────────────
distiller/node_modules/
distiller/out/
distiller/resources/icon.png

# ── Claude Code session files (temp paths, user-specific permissions) ────────
.claude/

# ── Stale root lockfile (no real root package.json) ──────────────────────────
/package-lock.json

# ── OS & editor noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
